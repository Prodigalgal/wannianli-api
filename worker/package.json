{
  "name": "wannianli-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:deep": "node ../tools/deep-validate.mjs",
    "check": "npm test && wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.118.0"
  }
}
